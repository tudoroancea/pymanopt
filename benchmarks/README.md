# pymanopt benchmarking

- import time
- autodiff time
- optimization time
- check numerical correctedness


- all in float64 to have comparable results
- code should be the same for both `master` and `dev` => we specify the backend by decorating the cost function
- choose a subset of optimization


uv venv .venv-master-numpy


uv venv .venv-master-numpy
